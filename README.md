# 🌾 IQ-FARM: نظام الزراعة الذكية للمزارعين العراقيين

## نظرة عامة

IQ-FARM هو نظام توصيات محاصيل ذكي وسهل الاستخدام، مصمم خصيصاً للمزارعين العراقيين. يستخدم تحليل بيانات التربة والطقس والمياه البسيط لتقديم توصيات محاصيل مبنية على:

- **بيانات التربة الفعلية** من 14 منطقة عراقية
- **متطلبات المحاصيل** لـ 26 محصول مختلف
- **تحليل سهل** دون الحاجة إلى خوارزميات معقدة
- **واجهة Telegram** سهلة الاستخدام مع لوحات مفاتيح Inline

## المميزات الرئيسية

✅ **توصيات ذكية** - توصيات محاصيل مبنية على بيانات حقيقية
✅ **واجهة سهلة** - استخدام Telegram مع Inline Keyboard
✅ **رسوم بيانية** - عرض البيانات بصرياً مع Matplotlib
✅ **قاعدة بيانات عراقية شاملة** - بيانات تربة من 14 محافظة
✅ **لوحة إدارة** - إمكانية إضافة بيانات تربة جديدة بصيغة JSON
✅ **تحليل التربة** - رادار chart لتحليل جودة التربة
✅ **بدون خوارزميات معقدة** - فقط مقارنات بسيطة وسهلة الفهم

## المناطق المغطاة

- البصرة 🌊
- الناصرية
- بغداد
- كركوك
- الموصل ⛰️
- ديالى
- الأنبار 🏜️
- السليمانية
- أربيل
- الحلة
- كربلاء
- واسط
- المثنى
- ميسان

## المحاصيل المدعومة (26 محصول)

### محاصيل التمر والعنب
- نخيل التمر
- التمر
- الزيتون
- اللوز
- الرمان

### محاصيل الحبوب
- القمح
- الشعير
- الأرز
- الذرة
- العدس
- السمسم
- دوار الشمس

### محاصيل الخضروات
- الطماطم
- الخيار
- الباذنجان
- البصل
- البطاطس
- الفلفل
- الثوم

### محاصيل أخرى
- الحمضيات
- الشمام
- البطيخ
- الكوسة
- الأرز
- القطن
- قصب السكر

## المتطلبات

```bash
pip install -r requirements.txt
```

## الحزم المستخدمة

| الحزمة | الإصدار | الاستخدام |
|--------|---------|----------|
| `python-telegram-bot` | 20.7 | واجهة Telegram |
| `pandas` | 2.1.4 | معالجة البيانات |
| `numpy` | 1.26.3 | العمليات الحسابية |
| `matplotlib` | 3.8.2 | الرسوم البيانية |

## التثبيت والتشغيل

### 1. استنساخ المستودع أو نسخ الملفات

```bash
git clone <repository-url>
cd IQ_FARM
```

### 2. إنشاء بيئة افتراضية

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# أو
venv\Scripts\activate  # Windows
```

### 3. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 4. إعداد الإعدادات

**الخطوة 1: الحصول على Telegram Bot Token**
1. تحدث مع [@BotFather](https://t.me/botfather) على Telegram
2. أرسل الأمر `/newbot`
3. اتبع التعليمات واحصل على التوكن

**الخطوة 2: الحصول على Admin ID**
1. تحدث مع [@userinfobot](https://t.me/userinfobot)
2. سيعطيك رقم معرفك (User ID)

**الخطوة 3: تحديث الإعدادات**
فتح `iq_farm_main.py` وغيّر:

```python
TOKEN = "YOUR_BOT_TOKEN_HERE"
ADMIN_ID = YOUR_ADMIN_ID_HERE
```

### 5. تشغيل البوت

```bash
python iq_farm_main.py
```

ستظهر رسالة:
```
🚀 Starting IQ-FARM Bot...
```

## كيفية الاستخدام

### للمزارعين العاديين

1. **ابدأ المحادثة**
   ```
   /start
   ```

2. **اختر منطقتك**
   - اضغط على "🌍 اختر منطقة"
   - اختر منطقتك من القائمة

3. **احصل على التوصيات**
   - سيظهر لك:
     - قائمة أفضل 5 محاصيل موصى بها
     - درجة التوصية لكل محصول
     - رسم بياني للمقارنة
     - رادار chart لتحليل التربة

### للمزارعين المتقدمين (إدخال مخصص)

1. اختر "📊 إدخال بيانات مخصصة"
2. أدخل:
   - درجة الحرارة (بالدرجات المئوية)
   - معدل الهطول السنوي (بالملليمتر)
   - درجة حموضة التربة (pH)

### للمشرفين (لوحة الإدارة)

1. اضغط على "🔐 لوحة الإدارة" (متاح فقط للمشرفين)
2. اختر "➕ إضافة بيانات تربة"
3. أرسل البيانات بصيغة JSON:

```json
{
  "region": "منطقة جديدة",
  "soil_type": "نوع التربة",
  "ph": 7.5,
  "nitrogen_ppm": 50,
  "phosphorus_ppm": 25,
  "potassium_ppm": 260,
  "moisture_content_percent": 30,
  "organic_matter_percent": 2.5,
  "temperature_celsius": 27,
  "rainfall_mm_annual": 200
}
```

## هيكل الملفات

```
IQ_FARM/
├── iq_farm_main.py          # الملف الرئيسي - البوت والتطبيق
├── config.py                 # ملف الإعدادات
├── requirements.txt          # المتطلبات
├── README.md                 # هذا الملف
└── datasets/
    ├── soil_data.csv        # بيانات التربة العراقية (26 صف)
    └── crop_data.csv        # متطلبات المحاصيل (26 محصول)
```

## البيانات

### بيانات التربة (datasets/soil_data.csv)

تتضمن 26 عينة تربة من 14 منطقة عراقية مع المعاملات التالية:

| العمود | الوصف | الوحدة |
|-------|--------|--------|
| `region` | اسم المنطقة | نص |
| `soil_type` | نوع التربة | نص |
| `ph` | درجة الحموضة | 5.5-8.5 |
| `nitrogen_ppm` | تركيز النيتروجين | جزء في المليون |
| `phosphorus_ppm` | تركيز الفسفور | جزء في المليون |
| `potassium_ppm` | تركيز البوتاسيوم | جزء في المليون |
| `moisture_content_percent` | نسبة الرطوبة | نسبة مئوية |
| `organic_matter_percent` | نسبة المادة العضوية | نسبة مئوية |
| `temperature_celsius` | درجة الحرارة | درجة مئوية |
| `rainfall_mm_annual` | الهطول السنوي | ملليمتر |

### بيانات المحاصيل (datasets/crop_data.csv)

تتضمن متطلبات 26 محصول:

| العمود | الوصف |
|-------|--------|
| `crop_name` | اسم المحصول |
| `min_temperature` | الحد الأدنى للحرارة |
| `max_temperature` | الحد الأقصى للحرارة |
| `min_ph` | الحد الأدنى للحموضة |
| `max_ph` | الحد الأقصى للحموضة |
| `min_nitrogen_ppm` | الحد الأدنى للنيتروجين |
| `min_phosphorus_ppm` | الحد الأدنى للفسفور |
| `min_potassium_ppm` | الحد الأدنى للبوتاسيوم |
| `min_moisture_percent` | الحد الأدنى للرطوبة |
| `min_rainfall_mm` | الحد الأدنى للهطول |

## خوارزمية التوصية

النظام يستخدم **خوارزمية تصنيف بسيطة وسهلة الفهم**:

```
درجة التوصية = 0

1. درجة الحرارة (0-25 نقطة)
   إذا كانت درجة الحرارة ضمن نطاق المحصول → 25 نقطة

2. درجة الحموضة (0-20 نقطة)
   إذا كانت pH ضمن النطاق → 20 نقطة

3. النيتروجين (0-15 نقطة)
   إذا كان النيتروجين ≥ 80% من الحد الأدنى → 15 نقطة

4. الهطول (0-20 نقطة)
   إذا كان الهطول ≥ 70% من الحد الأدنى → 20 نقطة

5. الرطوبة (0-20 نقطة)
   إذا كانت الرطوبة ≥ 70% من الحد الأدنى → 20 نقطة

الإجمالي = مجموع جميع النقاط (من 0 إلى 100)

التوصيات:
- درجة ≥ 40: نوصي به
- درجة < 40: لا نوصي به
```

## الرسوم البيانية

النظام ينتج نوعين من الرسوم البيانية:

### 1. رسم بياني العمود (Bar Chart)
- يعرض أفضل 5 محاصيل موصى بها
- ألوان حسب درجة الأفضلية:
  - 🟢 أخضر: درجة ≥ 80% (ممتاز)
  - 🟡 أصفر: درجة 60-80% (جيد)
  - 🔴 أحمر: درجة < 60% (مقبول)

### 2. رسم بياني الرادار (Radar Chart)
- يعرض تحليل جودة التربة
- 5 محاور: pH، النيتروجين، الفسفور، البوتاسيوم، الرطوبة
- يساعد في فهم نقاط القوة والضعف في التربة

## العمليات المدعومة

### للمستخدمين العاديين
- ✅ اختيار منطقة والحصول على التوصيات
- ✅ إدخال بيانات تربة مخصصة
- ✅ عرض الإحصائيات والرسوم البيانية
- ✅ مشاهدة معلومات التربة التفصيلية
- ✅ قراءة "حول النظام"

### للمشرفين (Admin)
- ✅ جميع عمليات المستخدمين العاديين
- ✅ إضافة بيانات تربة جديدة
- ✅ عرض جميع البيانات المخزنة
- ✅ عرض إحصائيات الاستخدام

## استكشاف الأخطاء

### "Bot token is invalid"
**الحل**: تأكد من توكن البوت صحيح في `iq_farm_main.py`

### "No soil data found for region"
**الحل**: البيانات تُحمّل من `datasets/soil_data.csv` - تأكد من وجود الملف

### "JSON format error when adding data"
**الحل**: تأكد من صيغة JSON صحيحة وجميع الحقول المطلوبة موجودة

### الرسوم البيانية لا تظهر
**الحل**: تأكد من وجود Matplotlib مثبت: `pip install matplotlib`

## كيفية توسيع النظام

### إضافة محصول جديد
1. فتح `datasets/crop_data.csv`
2. أضف صف جديد مع متطلبات المحصول
3. احفظ الملف - سيتم تحميله تلقائياً

### إضافة منطقة جديدة
1. استخدم لوحة الإدارة
2. أرسل بيانات التربة الجديدة بصيغة JSON
3. ستُضاف تلقائياً إلى `datasets/soil_data.csv`

### تخصيص الرسوم البيانية
عدّل ألوان وحجم الخطوط في `VisualizationManager` في `iq_farm_main.py`:

```python
colors = ['#2ecc71', '#f39c12', '#e74c3c']  # الألوان
ax.set_figsize(12, 8)  # حجم الرسم
```

## ملاحظات مهمة

⚠️ **استبدال التوكن والـ Admin ID**: لا تشارك توكن البوت أو User ID مع أحد

⚠️ **الخصوصية**: البيانات تُخزّن محلياً في ملفات CSV

⚠️ **الأداء**: النظام مُحسّن للاستخدام من قِبل مئات المستخدمين

⚠️ **الدقة**: التوصيات مبنية على بيانات تاريخية وقد تختلف حسب الظروف المحلية

## المساهمة والتطوير

لإضافة ميزات جديدة:
1. عدّل `iq_farm_main.py`
2. أضف وظائف جديدة
3. اختبر المميزات الجديدة

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر

## التواصل والدعم

للمساعدة والاستفسارات، يرجى التواصل عبر Telegram

---

**تم تطويره بعناية للمزارعين العراقيين 🌾**

آخر تحديث: ديسمبر 2025
